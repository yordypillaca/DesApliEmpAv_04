<UserControl x:Class="NeptunoApp.Views.ProductosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:NeptunoApp.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ProductosViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Refrescar" Command="{Binding CargarCommand}" />
            <Button Content="Nuevo" Command="{Binding NuevoCommand}" />
            <Button Content="Editar" Command="{Binding EditarCommand}" />
            <Button Content="Eliminar" Command="{Binding EliminarCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Productos}"
                  SelectedItem="{Binding Seleccionado}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProductoID}" Width="50" />
                <DataGridTextColumn Header="Producto" Binding="{Binding NombreProducto}" Width="*" />
                <DataGridTextColumn Header="Categoría" Binding="{Binding NombreCategoria}" Width="140" />
                <DataGridTextColumn Header="Proveedor" Binding="{Binding NombreProveedor}" Width="180" />
                <DataGridTextColumn Header="Presentación" Binding="{Binding CantidadPorUnidad}" Width="110" />
                <DataGridTextColumn Header="Precio" Binding="{Binding PrecioUnidad, StringFormat={}{0:N2}}" Width="80" />
                <DataGridTextColumn Header="Stock" Binding="{Binding UnidadesEnExistencia}" Width="70" />
                <DataGridCheckBoxColumn Header="Disc." Binding="{Binding Descontinuado}" Width="60" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="{Binding MensajeEstado}" Margin="0,8,0,0" />
    </Grid>
</UserControl>
