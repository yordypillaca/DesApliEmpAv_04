<Window x:Class="NeptunoApp.Views.CategoriaEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NeptunoApp.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:CategoriaEditorViewModel}"
        Title="{Binding Titulo}" Height="280" Width="460"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="{Binding Titulo}"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,12" />

        <TextBlock Grid.Row="1" Text="Nombre:" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding NombreCategoria, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="2" Text="Descripción:" VerticalAlignment="Top" Margin="0,4,0,0" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Descripcion, UpdateSourceTrigger=PropertyChanged}"
                 Height="70" TextWrapping="Wrap" AcceptsReturn="True" />

        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Guardar" Command="{Binding AceptarCommand}" IsDefault="True" />
            <Button Content="Cancelar" Command="{Binding CancelarCommand}" IsCancel="True" />
        </StackPanel>

        <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Text="{Binding MensajeEstado}"
                   Foreground="Firebrick" Margin="0,10,0,0" TextWrapping="Wrap" />
    </Grid>
</Window>
