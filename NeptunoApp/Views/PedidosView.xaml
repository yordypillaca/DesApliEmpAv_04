<UserControl x:Class="NeptunoApp.Views.PedidosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:NeptunoApp.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PedidosViewModel}">
    <TabControl SelectedIndex="{Binding TabSeleccionado}">
        <TabItem Header="Mantenimiento">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <Button Content="Refrescar" Command="{Binding CargarCommand}" />
                    <Button Content="Nuevo" Command="{Binding NuevoCommand}" />
                    <Button Content="Editar" Command="{Binding EditarCommand}" />
                    <Button Content="Eliminar" Command="{Binding EliminarCommand}" />
                </StackPanel>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Pedidos}"
                          SelectedItem="{Binding Seleccionado}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding PedidoID}" Width="50" />
                        <DataGridTextColumn Header="Fecha" Binding="{Binding FechaPedido, StringFormat={}{0:dd/MM/yyyy}}" Width="100" />
                        <DataGridTextColumn Header="Cliente" Binding="{Binding NombreCliente}" Width="200" />
                        <DataGridTextColumn Header="Empleado" Binding="{Binding NombreEmpleado}" Width="160" />
                        <DataGridTextColumn Header="Destinatario" Binding="{Binding Destinatario}" Width="180" />
                        <DataGridTextColumn Header="Ciudad" Binding="{Binding CiudadDestino}" Width="110" />
                        <DataGridTextColumn Header="Envío" Binding="{Binding FechaEnvio, StringFormat={}{0:dd/MM/yyyy}}" Width="100" />
                        <DataGridTextColumn Header="Transportista" Binding="{Binding NombreTransportista}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Grid.Row="2" Text="{Binding MensajeEstado}" Margin="0,8,0,0" />
            </Grid>
        </TabItem>

        <TabItem Header="Reportes">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="White" Padding="10" Margin="0,0,0,8" CornerRadius="6">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Desde:" VerticalAlignment="Center" Margin="0,0,8,8" />
                        <DatePicker SelectedDate="{Binding FechaInicio}" Width="140" Margin="0,0,16,0" />
                        <TextBlock Text="Hasta:" VerticalAlignment="Center" Margin="0,0,8,8" />
                        <DatePicker SelectedDate="{Binding FechaFin}" Width="140" Margin="0,0,16,0" />
                        <Button Content="Consultar" Command="{Binding ConsultarReporteCommand}" />
                    </StackPanel>
                </Border>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Reporte}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Pedido" Binding="{Binding PedidoID}" Width="70" />
                        <DataGridTextColumn Header="Fecha" Binding="{Binding FechaPedido, StringFormat={}{0:dd/MM/yyyy}}" Width="100" />
                        <DataGridTextColumn Header="Destinatario" Binding="{Binding Destinatario}" Width="180" />
                        <DataGridTextColumn Header="Ciudad" Binding="{Binding CiudadDestino}" Width="110" />
                        <DataGridTextColumn Header="Producto" Binding="{Binding NombreProducto}" Width="*" />
                        <DataGridTextColumn Header="Precio" Binding="{Binding PrecioUnidad, StringFormat={}{0:N2}}" Width="80" />
                        <DataGridTextColumn Header="Cant." Binding="{Binding Cantidad}" Width="60" />
                        <DataGridTextColumn Header="Desc." Binding="{Binding Descuento, StringFormat={}{0:P0}}" Width="70" />
                        <DataGridTextColumn Header="Importe" Binding="{Binding Importe, StringFormat={}{0:N2}}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Grid.Row="2" Text="{Binding MensajeReporte}" Margin="0,8,0,0" FontWeight="SemiBold" />
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>
