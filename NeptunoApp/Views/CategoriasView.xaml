<UserControl x:Class="NeptunoApp.Views.CategoriasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:NeptunoApp.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:CategoriasViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Refrescar" Command="{Binding CargarCommand}" />
            <Button Content="Nueva" Command="{Binding NuevoCommand}" />
            <Button Content="Editar" Command="{Binding EditarCommand}" />
            <Button Content="Eliminar" Command="{Binding EliminarCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Categorias}"
                  SelectedItem="{Binding Seleccionada}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CategoriaID}" Width="50" />
                <DataGridTextColumn Header="Nombre" Binding="{Binding NombreCategoria}" Width="220" />
                <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="{Binding MensajeEstado}" Margin="0,8,0,0" />
    </Grid>
</UserControl>
