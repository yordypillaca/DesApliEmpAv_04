<UserControl x:Class="NeptunoApp.Views.ProveedoresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:NeptunoApp.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ProveedoresViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="10" Margin="0,0,0,8" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Contacto:" VerticalAlignment="Center" Margin="0,0,8,8" />
                <TextBox Grid.Column="1" Text="{Binding FiltroContacto, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" />

                <TextBlock Grid.Column="2" Text="Ciudad:" VerticalAlignment="Center" Margin="0,0,8,8" />
                <TextBox Grid.Column="3" Text="{Binding FiltroCiudad, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,12,0" />

                <StackPanel Grid.Column="4" Orientation="Horizontal">
                    <Button Content="Buscar" Command="{Binding BuscarCommand}" />
                    <Button Content="Limpiar" Command="{Binding LimpiarCommand}" />
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Refrescar" Command="{Binding CargarCommand}" />
            <Button Content="Nuevo" Command="{Binding NuevoCommand}" />
            <Button Content="Editar" Command="{Binding EditarCommand}" />
            <Button Content="Eliminar" Command="{Binding EliminarCommand}" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Proveedores}"
                  SelectedItem="{Binding Seleccionado}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProveedorID}" Width="50" />
                <DataGridTextColumn Header="Compañía" Binding="{Binding CompaniaNombre}" Width="200" />
                <DataGridTextColumn Header="Contacto" Binding="{Binding NombreContacto}" Width="140" />
                <DataGridTextColumn Header="Cargo" Binding="{Binding CargoContacto}" Width="160" />
                <DataGridTextColumn Header="Ciudad" Binding="{Binding Ciudad}" Width="110" />
                <DataGridTextColumn Header="País" Binding="{Binding Pais}" Width="80" />
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="120" />
                <DataGridTextColumn Header="Dirección" Binding="{Binding Direccion}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding MensajeEstado}" Margin="0,8,0,0" />
    </Grid>
</UserControl>
